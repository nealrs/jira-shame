services:
  jira-shame:
    build: .
    ports:
      - "1337:3000"
    environment:
      - JIRA_HOST=audicus.atlassian.net
      - JIRA_EMAIL=${JIRA_EMAIL}
      - JIRA_API_TOKEN=${JIRA_API_TOKEN}
      - BOARD_ID=7
    restart: unless-stopped
